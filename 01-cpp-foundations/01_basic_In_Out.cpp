#include <iostream>  // used for inputs/outputs
#include <math.h>    // used for mathematical functions
using namespace std;
// if "using namespace std;" is not use then we have to use std::
// std::cout << "Hey there, World!" << std::endl;

int main(){
    // Output
    cout << "\nHey there, World!" << endl;

    // Input
    int x, y;
    cin >> x >> y;
    cout << "Value of x: " << x << "\nValue of y: " << y;

    return 0;
}